<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.creditplus.p2p.dao.UserDao">
    <insert id="insertUser" parameterType="UserVO">
       insert into user_t(id,username, password,remark) values(#{id}, #{username},#{password},#{remark})
    </insert>
   
    <update id="updateUser" parameterType="UserVO">
       update user_t set username=#{username}, password=#{password} where id=#{id}
    </update>
   
    <select id="getUserById" parameterType="String" resultType="UserVO">
       select * from user_t where id=#{id}
    </select>
   
    <delete id="deleteUserById" parameterType="String">
       delete from user_t where id=#{id}
    </delete>
</mapper>